{
    "name": "roomiematch",
    "type": "web",
    "env": "docker",
    "dockerfilePath": "./Dockerfile",
    "healthCheckPath": "/api/health",
    "envVars": [
        { "key": "APP_ENV", "value": "production" }
    ]
}
